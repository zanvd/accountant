<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Accountant{{if .Title}} - {{.Title}}{{end}}</title>
    <link href="https://cdn.jsdelivr.net/npm/normalize.css@8.0.1/normalize.css" rel="stylesheet"/>
    <link href="{{.Routes.BaseUrl}}/static/style.css" rel="stylesheet">
    <link href="{{.Routes.BaseUrl}}/static/desktop.css" media="screen and (min-width: 750px)" rel="stylesheet"/>
    <link href="{{.Routes.BaseUrl}}/static/mobile.css" media="screen and (max-width: 750px)" rel="stylesheet"/>
    <link href="{{.Routes.BaseUrl}}/static/dtsel-picker/dtsel.css" rel="stylesheet"/>
    <script src="{{.Routes.BaseUrl}}/static/script.js" defer></script>
    <script src="{{.Routes.BaseUrl}}/static/dtsel-picker/dtsel.js" defer></script>
</head>
<body>
<header>
    <nav>
        <div id="menu-brand">
            <a href="{{url "home" .Routes}}">Accountant</a>
        </div>
        <input id="menu-button" type="checkbox"/>
        <label id="menu-burger" for="menu-button">
            <svg width="1.5em" height="1.5em" viewBox="0 0 16 16" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
                <path fill-rule="evenodd"
                      d="M2 12.5a.5.5 0 0 1 .5-.5h11a.5.5 0 0 1 0 1h-11a.5.5 0 0 1-.5-.5zm0-3a.5.5 0 0 1 .5-.5h11a.5.5 0 0 1 0 1h-11a.5.5 0 0 1-.5-.5zm0-3a.5.5 0 0 1 .5-.5h11a.5.5 0 0 1 0 1h-11a.5.5 0 0 1-.5-.5zm0-3a.5.5 0 0 1 .5-.5h11a.5.5 0 0 1 0 1h-11a.5.5 0 0 1-.5-.5z"
                />
            </svg>
        </label>
        <div id="menu">
            {{if .Session.LoggedIn}}
                <a href="{{url "dashboard" .Routes}}">Dashboard</a>
                <a href="{{url "category-list" .Routes}}">Categories</a>
                <a href="{{url "transaction-list" .Routes}}">Transactions</a>
                <a href="{{url "transaction-template-list" .Routes}}">Transaction templates</a>
                <a href="{{url "user-profile" .Routes}}">{{.Session.User.Username}}</a>
                <a href="{{url "auth-logout" .Routes}}">Logout</a>
            {{else}}
                <a href="{{url "auth-login" .Routes}}">Login</a>
            {{end}}
        </div>
    </nav>
</header>
<main>
    {{if .ErrorStatus}}
        {{template "error" .}}
    {{else}}
        {{template "content" .}}
    {{end}}
    <div id="deletion-dialog-container" class="hidden">
        <div id="deletion-dialog">
            <h2 id="deletion-title"></h2>
            <hr/>
            <p id="deletion-text"></p>
            <hr/>
            <div class="dialog-actions">
                <button id="deletion-submit" class="btn btn-icon" type="submit">
                    <svg class="feather">
                        <use xlink:href="{{.Routes.BaseUrl}}/static/icons/feather-sprite.svg#check"/>
                    </svg>
                </button>
                <button id="deletion-cancel" class="btn btn-icon btn-cancel" type="button">
                    <svg class="feather">
                        <use xlink:href="{{.Routes.BaseUrl}}/static/icons/feather-sprite.svg#x"/>
                    </svg>
                </button>
            </div>
        </div>
    </div>
</main>
<footer>
    <p>
        &copy; <a href="http://www.devexp.co" target="_blank">devexp</a>
    </p>
</footer>
</body>
</html>
