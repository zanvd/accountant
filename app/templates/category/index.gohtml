{{define "content"}}
    <section>
        <article class="page-title">
            <h1>Categories</h1>
            <div class="actions">
                <a class="btn btn-icon btn-add" href="{{url "category-add" .Routes}}">
                    <svg class="feather">
                        <use xlink:href="{{.Routes.BaseUrl}}/static/icons/feather-sprite.svg#plus"/>
                    </svg>
                </a>
            </div>
        </article>
        <article>
            {{if not .Data}}
                <p>No categories.</p>
            {{else}}
                <table>
                    <thead>
                    <tr>
                        <th>Name</th>
                        <th>Description</th>
                        <th></th>
                    </tr>
                    </thead>
                    <tbody>
                    {{range $category := .Data}}
                        <tr>
                            <td>
                                <span class="pill"
                                      style="background-color: {{$category.Color}}; color: {{$category.TextColor}};"
                                >
                                    {{$category.Name}}
                                </span>
                            </td>
                            <td>{{$category.Description}}</td>
                            <td class="table-action-cell">
                                <a class="btn btn-icon btn-edit" href="{{url "category-edit" $.Routes}}/{{$category.Id}}">
                                    <svg class="feather">
                                        <use xlink:href="{{$.Routes.BaseUrl}}/static/icons/feather-sprite.svg#edit"/>
                                    </svg>
                                </a>
                                <a class="btn btn-icon btn-view" href="{{url "category-view" $.Routes}}/{{$category.Id}}">
                                    <svg class="feather">
                                        <use xlink:href="{{$.Routes.BaseUrl}}/static/icons/feather-sprite.svg#search"/>
                                    </svg>
                                </a>
                                <a class="btn btn-icon btn-delete confirm-deletion"
                                   href="{{url "category-delete" $.Routes}}/{{$category.Id}}"
                                   data-deletion-name="{{$category.Name}}"
                                   data-deletion-title="Category"
                                >
                                    <svg class="feather">
                                        <use xlink:href="{{$.Routes.BaseUrl}}/static/icons/feather-sprite.svg#trash"/>
                                    </svg>
                                </a>
                            </td>
                        </tr>
                    {{end}}
                    </tbody>
                </table>
            {{end}}
        </article>
    </section>
{{end}}
