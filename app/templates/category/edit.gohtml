{{define "content"}}
    <section>
        <article class="page-title">
            <h1>Categories</h1>
            <div class="actions">
                <a class="btn btn-icon btn-view" href="{{url "category-view" .Routes}}/{{.Data.Id}}">
                    <svg class="feather">
                        <use xlink:href="{{.Routes.BaseUrl}}/static/icons/feather-sprite.svg#search"/>
                    </svg>
                </a>
                <a class="btn btn-icon btn-delete confirm-deletion"
                   href="{{url "category-delete" .Routes}}/{{.Data.Id}}"
                   data-deletion-name="{{.Data.Name}}"
                   data-deletion-title="Category"
                >
                    <svg class="feather">
                        <use xlink:href="{{.Routes.BaseUrl}}/static/icons/feather-sprite.svg#trash"/>
                    </svg>
                </a>
                <a class="btn btn-icon btn-list" href="{{url "category-list" .Routes}}">
                    <svg class="feather">
                        <use xlink:href="{{.Routes.BaseUrl}}/static/icons/feather-sprite.svg#list"/>
                    </svg>
                </a>
            </div>
        </article>
        <article>
            <form action="{{url "category-edit" .Routes}}/{{.Data.Id}}" method="post">
                <h2>{{.Data.Name}}</h2>
                <div class="form-group">
                    <label for="name">Name:</label>
                    <input id="name" name="name" placeholder="Name" required type="text" value="{{.Data.Name}}"/>
                </div>
                <div class="form-group">
                    <label for="color">Color:</label>
                    <div class="color-input-container">
                        <input id="color" name="color" required type="color" value="{{.Data.Color}}"/>
                    </div>
                </div>
                <div class="form-group">
                    <label for="text-color">Text Color:</label>
                    <div class="color-input-container">
                        <input id="text-color" name="text-color" required type="color" value="{{.Data.TextColor}}"/>
                    </div>
                </div>
                <div class="form-group">
                    <label for="description">Description:</label>
                    <input id="description"
                           name="description"
                           placeholder="Description"
                           type="text"
                           value="{{.Data.Description}}"
                    />
                </div>
                <div class="form-actions">
                    <button class="btn btn-icon" type="submit">
                        <svg class="feather">
                            <use xlink:href="{{.Routes.BaseUrl}}/static/icons/feather-sprite.svg#check"/>
                        </svg>
                    </button>
                    <button class="btn btn-icon btn-cancel go-back" type="button">
                        <svg class="feather">
                            <use xlink:href="{{.Routes.BaseUrl}}/static/icons/feather-sprite.svg#x"/>
                        </svg>
                    </button>
                </div>
            </form>
        </article>
    </section>
{{end}}
