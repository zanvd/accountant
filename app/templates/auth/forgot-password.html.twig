{% extends 'base.html.twig' %}
{% block title %}Forgot password | {{ parent() }}{% endblock %}
{% block content %}
<article>
    {{ include('_heading.html.twig', {title: 'Forgot password'}) }}
    <section>
        {% for l, ms in app.flashes %}
            {% for m in ms %}
                <div class="{{ l }}">{{ m }}</div>
            {% endfor %}
        {% endfor %}
        {{ form(form) }}
    </section>
</article>
{% endblock %}
